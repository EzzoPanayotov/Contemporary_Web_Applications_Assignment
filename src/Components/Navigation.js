import SideBarLinks from "./SideBarLinks";

const Navigation = () =>{
    
    return(
        <nav className='navigation'>
            <SideBarLinks/>
        </nav>
    );
}

export default Navigation